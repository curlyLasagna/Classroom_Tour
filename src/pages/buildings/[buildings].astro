---
import type { GetStaticPaths } from "astro";
export async function const getStaticPaths = (() => {
    return [
        params: {},
        props: {},
    ];
}) satisfies GetStaticPaths;
const { ?? } = Astro.params;
const { ?? } = Astro.props;
---